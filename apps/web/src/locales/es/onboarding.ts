import type { Messages } from '../en';

/** onboarding — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const onboarding: Messages['onboarding'] = {};
