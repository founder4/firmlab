import type { Messages } from '../en';

/** imageDetail — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const imageDetail: Messages['imageDetail'] = {};
