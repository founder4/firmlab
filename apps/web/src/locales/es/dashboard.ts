import type { Messages } from '../en';

/** dashboard — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const dashboard: Messages['dashboard'] = {};
