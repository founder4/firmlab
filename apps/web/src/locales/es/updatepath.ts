import type { Messages } from '../en';

/** updatepath — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const updatepath: Messages['updatepath'] = {};
