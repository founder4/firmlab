import type { Messages } from '../en';

/** simulation — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const simulation: Messages['simulation'] = {};
