import type { Messages } from '../en';

/** testbench — Spanish. Typed against the English catalogue, so an untranslated key cannot ship silently. */
export const testbench: Messages['testbench'] = {};
